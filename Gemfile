source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins

# Ruby 3.4+ removed these from the default gems; github-pages pins an old
# Jekyll that still expects them to just be there. Local dev only — GitHub
# Pages' own build environment doesn't need this.
gem "csv"
gem "webrick"
gem "bigdecimal"
gem "logger"
gem "base64"
